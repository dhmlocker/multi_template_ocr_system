# Use the exact versions verified for this project.
# For Windows GPU, install the CUDA-matched paddlepaddle-gpu wheel first,
# then install the remaining packages from requirements.lock.txt.
-r requirements.lock.txt
